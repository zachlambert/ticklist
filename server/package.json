{
  "name": "ticklist-server",
  "version": "1.0.0",
  "description": "Backend server for ticklist app",
  "private": true,
  "type": "module",
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.5",
    "pg": "^8.11.0"
  },
  "devDependencies": {
  },
  "scripts": {
    "start": "node server.js",
    "build-image": "docker build -t ticklist-server:latest ."
  }
}
